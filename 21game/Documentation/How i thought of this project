How i thought of this project:
First i had the idea of the player entering the numbers then the computer then it'll be added to an array which will be checked constantly through the game loop but i thought that that was bad since it may take long for checking, so i had a much better idea with better time complexity at least from what i think by using functions:

i had 3 elements to make:
1. game rules
2. player
3. computer play

first the player function which has the entire player code:

arr[#21numbers] "this contains the game array which will contain every number entered"

int(input)
list -> check if len <= 3

then the game rules:

if player input:
	index 0 = index 1 - 1 (when index i mean the integer itself, and not the index of the array)
	and the same for the rest of the nums
if all True then "".join(playerInput).append(game_array)

then later I did the same for the computer play with the game rules in mind and a random num to check for the length of it's play
