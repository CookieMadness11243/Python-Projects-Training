How i made this project:

first of all i brought the idea on a notebook and i wrote the following:
```
variable = word = list(word)
guesses = 0
chances = len(word) + 1
word2 = word
for i in range(len(word)):
	word2[i] = "_"
import radom
num = random.random(0,len(word))
num2 = -----------------------
word2[num] = word[num]
----[---2] = ----------
print--
```
after that i included a visualization for how the code may work

then i worked on the rest "inputs and outputs"
```
input = "guess"
while guess != chances:
for i in range(len(word)):
	if input == word[i]:
		print("correct!")
		word2[i] = word[i]
		print(word2)
	else:
		print("sorry!")
		guess+=1
```
and this is how i visualized my code in my notebook to then convert it into real code
after that i programmed it and fixed any errors that occurred and finally finished the first project
